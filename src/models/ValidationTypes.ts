export enum ValidationTypes {
  REQUIRED = 'required',
  MIN = 'min',
  MAX = 'max',
  PATTERN = 'pattern',
  MIN_LENGTH = 'minLength',
  MAX_LENGTH = 'maxLength',
  EMAIL = 'email',
  URL = 'url',
  PHONE = 'phone',
  CUSTOM = 'custom'
}
